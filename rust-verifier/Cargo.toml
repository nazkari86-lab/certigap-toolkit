[package]
name = "certigap-verifier"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Standalone verifier for CertiGap proof-carrying artifacts"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
